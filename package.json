{
  "name": "digital-human",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "client": "yarn --cwd apps/frontend dev",
    "server": "yarn --cwd apps/backend start",
    "dev": "npm-run-all --parallel client server"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
